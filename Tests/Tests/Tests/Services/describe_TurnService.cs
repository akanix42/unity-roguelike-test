using NSpec;
using NSpec.Assertions;

class describe_TurnService : nspec {
  void when_adding_an_entity() {
    it["inserts entities with higher speed above those with lower speed"] = () => {
//      var contexts = new Contexts();
//      var turnService = new TurnService.SpeedSortComparer(contexts);
//      var entityId = 0;
//      var speed = 10;
//
//      for (var i = 0; i < 5; i++) {
//        turnService.AddEntity(entityId++, speed += 10);
//      }
//      
      
    };
  }
}